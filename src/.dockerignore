# 构建上下文(src/)排除清单
# 目标:镜像不带开发产物,更不允许把本地密钥/数据打进镜像层

# 虚拟环境与缓存
**/.venv
**/__pycache__
**/*.pyc
**/.pytest_cache
**/.ruff_cache

# 前端
**/node_modules
web/dist

# 测试与评测(镜像不需要)
server/tests
server/eval
node/tests

# 本地环境变量(含密钥,绝不入镜像)
**/.env
**/.env.*

# 本地数据(数据库/快照/密钥)
server/data
node/data

# 文档与工程文件(镜像不需要)
**/*.md
**/.gitignore
**/uv.lock
.git
