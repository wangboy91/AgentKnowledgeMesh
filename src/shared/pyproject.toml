[project]
name = "akm-shared"
version = "0.2.0"
license = { text = "MIT" }
description = "AgentKnowledgeMesh shared library (Markdown scanner)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["akm_shared"]
