1. 向量索引的缺口（我这次是故意划出范围的）
add-node-http-sync 里节点上传只入库 documents，没同步向量。也就是说：本地扫描的文档能被语义搜索，节点推送的文档不能。这是当时为了聚焦而窄化掉的——但它是真缺口还是「该做的下一步」？值得想清楚边界。

2. 读/写鉴权不对称
PUT 端点是 Bearer token 保护的，但 GET /api/nodes、GET /api/nodes/{id}/documents、GET /api/documents/* 全是裸奔的。token 只防写、不防读。这是有意为之（内网信任模型）还是疏忽？

3. token 生命周期为零
token 是 uuid4().hex，注册时生成、永久复用，没有过期、轮换、撤销。泄漏了怎么办？现在连个「吊销某个节点 token」的口子都没有。

4. 一个我还没验证的潜在坑
init_db() 里 Base.metadata.create_all 只 import 了 Document，没 import Node。那 nodes 表到底是怎么建出来的？如果某个部署路径没建这个表，注册就会炸——我想去确认一下真实行为。

5. 全量推送的重量
每次 sync_request 都推全文。知识库一大，这就是 O(全部字节) 的搬运。要不要改成 hash-first（先比哈希、只传变化）？


问题点：

字符计数，不是 token 计数。中英文混排时 500 字符对应多少 token 完全不可控，嵌入模型（doubao 那类）是按 token 收的，边界不对。
无视 Markdown 结构。表格、代码块、# 标题会被从中间硬切——表格切两半基本就废了，标题上下文丢失。对知识库这是最伤的一点。
没有把标题/文档名前置到 chunk。常见做法是每个 chunk 嵌成「# 标题\n...正文...」，让向量带上结构上下文，当前是裸正文。
尾部可能切出极小碎片 chunk（没有最小块保护）。

纯稠密、无混合检索。没有和关键词搜索（/api/search 的 BM25/LIKE）做融合（RRF 之类）。搜具体文件名、ID、代码 token、专有名词时，dense 经常漏——这是召回率的主要杀手。
每文档只取 1 个 chunk。一篇文档里两个不相关的段落同时相关时，只能命中一个。limit=5 实际是「最多 5 篇文档 × 各 1 块」，不是 5 块。
无相似度阈值。没有任何 score 门槛，永远返回 top-K，哪怕全是不相关（score≈0）也照回。precise 差，但 score 又原样返回给调用方，下游没人过滤。
K 偏小。RAG context 默认 limit=3（rag.py:55），召回窗口很窄，容易漏掉第 4~10 名里真正有用的。
无重排（rerank）。召回后直接按向量分数排，没有 cross-encoder 精排。

